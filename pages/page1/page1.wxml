<!-- pages/page1/page1.wxml -->
<view class="box">
  <view class="row" wx:for="{{[1,2,3,4,5,6,7,8]}}" wx:for-item="row">
    <view class="col" wx:for="{{[1,2,3,4,5,6,7,8]}}" wx:for-item="col" wx:if="{{col<=row}}">
      {{col}}*{{row}}={{row*col}}
    </view>
  </view>
</view>
<view class="" wx:if="{{h=='haha'}}">{{d.f}}</view>

<video class="" src=""></video>
<view bind:tap="onClick">点击我</view>
<view>sss33333</view>
